pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'cd to-do'
                sh 'pytest --junit-xml test-reports/results.xml'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }

        }

    }
}